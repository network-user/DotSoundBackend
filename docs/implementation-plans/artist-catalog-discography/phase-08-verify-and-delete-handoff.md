# Phase 8 из 8 — Проверка и удаление handoff

## Общий контекст задачи (вся цепочка 1–8)

Все предыдущие этапы реализованы. Этот этап — **приёмка** и **уборка временных артефактов** передачи контекста между чатами, чтобы в репозитории не оставались устаревшие инструкции.

Подробная матрица: [README.md](README.md).

## Роль этого этапа

**Этап 8 из 8 (финал).** Сквозной smoke-сценарий, фиксы только критичных багов обнаруженных при приёмке; **удаление** каталога `docs/implementation-plans/artist-catalog-discography/`.

## Границы (анти-конфликты)

| Делает этот этап | Не делает |
|------------------|-----------|
| Ручной/автоматический чеклист по README плана | Новые фичи каталога — только отдельная задача |
| `git rm -r docs/implementation-plans/artist-catalog-discography` или удаление через IDE | Удаление канонического `.cursor/plans/*.plan.md` вне репозитория — по усмотрению; в git обычно только папка handoff |

**Prerequisites:** этапы 1–7 завершены.

**Выход:** репозиторий без папки handoff; фича считается принятой.

---

## Цель

Сквозная проверка; удаление временных файлов передачи контекста.

## Что делать

1. Пройти сценарий: артист с `soundcloud_user_id` → resync → релизы на карточке → экран альбома → playback → админ правка порядка → повторный resync одного релиза.
2. Убедиться, что CI/локальные тесты зелёные.
3. **Удалить каталог** `docs/implementation-plans/artist-catalog-discography/` целиком (включая этот README).

## PROMPT для нового чата

```
Финальная проверка фичи каталога артиста в DotSoundBackend: прогони чеклист из phase-08-verify-and-delete-handoff.md (этап 8 из 8). Исправь только блокеры. Затем удали всю папку docs/implementation-plans/artist-catalog-discography/. Один коммит chore/docs по согласованию с веткой.
```

## Примечание

Если папка уже удалена ранее — этап считается выполненным.
