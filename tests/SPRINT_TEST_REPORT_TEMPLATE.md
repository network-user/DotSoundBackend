# Sprint test / coverage report (template)

Use one row (or section) per failure investigated during a sprint.

| Test / node id | Traceback (short) | Root cause | Fix (code vs test) | File(s) |
|----------------|-------------------|------------|--------------------|--------|
| | | | | |

## Optional: coverage snapshot

- Repository:
- Date:
- `pytest` command:
- Line coverage %:
- Top 5 uncovered modules (paths):

## Tech debt / follow-ups

- Manual / live-network tests not in CI:
- Flaky tests:
- Deferred coverage (document `omit` reasons):
