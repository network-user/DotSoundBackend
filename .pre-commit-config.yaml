repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
        # compose overlays use custom YAML tags (!reset / !override)
        args: ['--unsafe']
      - id: check-toml
      - id: check-added-large-files
        args: ['--maxkb=1024']
      - id: detect-private-key

# Lint / format hooks stay disabled until the Ruff / Mypy / Black backlog
# is green for public checkouts (see .github/workflows/ci.yml). Re-enable
# when ready:
#  - repo: https://github.com/astral-sh/ruff-pre-commit
#    rev: v0.5.7
#    hooks:
#      - id: ruff
#        args: [--fix, --exit-non-zero-on-fix]
#        files: ^app/
#      - id: ruff-format
#        files: ^app/
#
#  - repo: https://github.com/psf/black
#    rev: 24.10.0
#    hooks:
#      - id: black
#        args: [--line-length=79]
#        files: ^app/
