.env
__pycache__/
*.py[cod]
.mypy_cache/
.ruff_cache/
.pytest_cache/
.pytest_tmp/
dist/
build/
*.egg-info/
.idea/
.vscode/

# Coverage artifacts
.coverage
.coverage.*
coverage.json
coverage.xml
htmlcov/

# Misc local caches / artifacts
.vite/
*.log
*.bak

# Co-author history rewrite backups (local only)
.git-coauthor-backups/
.git-history-backups/
.git-release-backups/

# Ad-hoc import probes (never publish)
scripts/_tmp_*

# Frontend build output (regenerated by `cd frontend && npm run build`)
app/static/mini_app/

# Frontend node_modules
frontend/node_modules/

# Playwright E2E output (never commit run artifacts / traces)
frontend/test-results/
frontend/playwright-report/
.claude/
# Local chat context exports (do not commit)
context_delete.txt

# Debug-mode per-service log files (settings.debug=true only)
logs/

# Secrets / keys / local data (defense-in-depth — keep the template tracked)
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keystore
*.jks
secrets.json
credentials.json
service-account*.json
*.sqlite
*.sqlite3
*.db
*.tmp

# Virtual environments
venv/
.venv/
env/
